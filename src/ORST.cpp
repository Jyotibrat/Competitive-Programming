#include<iostream>
using namespace std;

class ORST
{   
    int T, N, M;
    int arr_A[];
    int arr_B[];
    int test_arr[];
    public:
        void Get_Data()
        {
            cin >> T >> endl;
            for(int i = 1;i<=T;i++)
            {
                test_arr[];
                cin >> N >> M >> endl;
                for(int j=0;j<N;j++)
                {
                    cin >> arr_A[j] >> " ";
                }
                for(int k=0;k<M;K++)
                {
                    cin >> arr_B[k] >> " ";
                }
                int last_element =arr_B[M-1];
                for(int l=last_element;l>=0;l--)
                {
                    for(int m=0;m<l;m++)
                    {
                        test_arr[]
                    }
                } 
            }
        }
};
int main()
{
    ORST obj;
    return 0;
}